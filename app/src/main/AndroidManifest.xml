<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.raf.projekat2.danijal_azerovic_RN8618_dusan_jerinic_RN8718">

    <application
        android:name=".application.ProjectApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_splash_student"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_splash_student_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.view.activities.LoginActivity" android:windowSoftInputMode="adjustPan"></activity>
        <activity
            android:name=".presentation.view.activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.view.activities.MainActivity" android:windowSoftInputMode="adjustPan"></activity>
    </application>

</manifest>